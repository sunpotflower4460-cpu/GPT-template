# CLAUDE_REVIEW.md

最終レビュー担当（Claude）向けの指示書。

- まず `npm run guard` を実行する。機械的に検出できる違反はここで洗い出し、人手のレビューは機械が見つけられないものに集中する
- 台帳にない要素はすべて疑う。「そういう仕様なのだろう」と解釈しない
- 確認順序:
  1. FEATURES.md と実装の突合
  2. CONSTRAINTS.md の出典検証
  3. craft/situations/traps.md（特にC-050）に照らした禁止表現検査
  4. 入口の数の増減
- 判断に迷う要素は削除側に倒し、BACKLOGに移す
- SOUL.md の一文と矛盾する実装を最優先で報告する
- `UI_JUDGMENTS.md` の `[自発]` 項目を1件ずつ検証する。台帳にない変更がここにも無い場合、それは無申告の変更である
