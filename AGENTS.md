# AGENTS.md

このファイルは GPT / Codex がリポジトリ編集の前に最初に読む憲法である。すべての個別指示より優先する。

## 0. 最初に（カナリア）

すべての応答の冒頭に、次のヘッダをそのまま出力すること。

```
【SOUL】<SOUL.md の一文をそのまま引用>
【PHASE】<PHASE.md の値>
【FEATURE】<今回触る承認済みID / なければ「なし」>
【増える入口】<この変更で増える画面・タブ・設定項目の数>
```

このヘッダのない応答は「憲法未読」とみなす。

## 1. 三原則

- **不明なとき** → 推測で埋めず `UNKNOWN` と記録する
- **思いついたとき** → 実装せず `BACKLOG.md` に書く
- **強化するとき** → 表に足さず、既存を深くする

## 2. 絶対禁止

- `FEATURES.md` に承認済みIDのない機能を実装すること
- `CONSTRAINTS.md` に出典（Q-ID）のない制約を追加すること
- `docs/04-design/tokens.css` にない色・サイズ値をハードコードすること
- ユーザー回答を要約・言い換えして `ANSWERS.md` に記録すること（原文ママのみ）
- `PHASE.md` を GPT 自身が書き換えること（フェーズ進行はユーザーの承認のみ）

## 3. フェーズと許可される行為

| Phase | 状態 | 許可される行為 |
|---|---|---|
| P0 | 受入 | 既存資材の読み取り、`INVENTORY.md` の作成のみ |
| P1 | 質問 | 質問と `ANSWERS.md` への記録のみ。コード禁止 |
| P2 | 設計 | `FEATURES.md` `DECISIONS.md` の起草。コード禁止 |
| P3 | 実装 | 承認済みIDの範囲のみ実装可 |
| P4 | 自己監査 | 差分の自己申告、`HANDOFF.md` 生成 |

P1 に `UNKNOWN` または未回答が1件でも残っている場合、P3 に進んではならない。

## 4. 受入の儀式

- 既存のコンセプトデモ / README を渡された場合 → まず `INVENTORY.md`（棚卸し）を作り、**機能1件ずつ**認識合わせをする。質問より棚卸しが先。
- ゼロから始める場合 → `QUESTIONS.md` の質問を上から順に聞く。

共通ルール:

- 1メッセージあたり質問は最大3問
- 機能確認は必ず1機能ずつ
- 質問には必ず選択肢（A/B/C + 自由記述）を添える

## 5. 実装のルール

- 実装ファイルの先頭に `@feature F-00X` コメントを必須にする
- 1 PR あたり: 新規の入口（画面・タブ・設定項目）は最大1、新規依存は0
- 補強の定義: 補強とは (a) 既存動線の信頼性 (b) エッジケースの挙動 (c) 手触り の3つのみを指す。**新規の入口を増やすことは補強ではない。**

## 6. UI のルール

色・サイズは `docs/04-design/tokens.css` の変数のみを使用する。禁止表現・必須ルールは `docs/04-design/UX_AXIS.md` を参照する。既定値の寄せ集めは禁止。

## 7. 応答フォーマット

ヘッダ（0章参照） → 本文 → 末尾に「今回 BACKLOG に逃がしたアイデア」欄。
